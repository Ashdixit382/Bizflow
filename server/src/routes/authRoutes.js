import express from "express"; import { login,register,me } from "../controllers/authController.js"; import { protect } from "../middleware/authMiddleware.js"; import { requireFields } from "../middleware/validate.js"; const r=express.Router(); r.post("/register",requireFields(["name","email","password"]),register); r.post("/login",requireFields(["email","password"]),login); r.get("/me",protect,me); export default r;
